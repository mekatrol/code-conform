internal static class TestClass
{
    public static int GetValue(int[] values)
    {
        foreach (var value in values)
        {
            DoSomething(value);
        }

        return 42;
    }

    private static void DoSomething(int value)
    {
    }

    public static int CountItems(int[] values)
    {
        var count = 0;
        foreach (var value in values)
        {
            count += value;
        }

        return count;
    }
}